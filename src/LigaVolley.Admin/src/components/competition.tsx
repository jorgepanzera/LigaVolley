import{NavLink}from'react-router-dom';import type{Competition,Status}from'../types/admin';
const labels:Record<Status,string>={Draft:'Borrador',Scheduled:'Programada',InProgress:'En juego',Finished:'Finalizada',Cancelled:'Cancelada'};
export function StatusBadge({status}:{status:Status}){return <span className={`badge ${status.toLowerCase()}`}>{labels[status]}</span>}
export function CompetitionHeader({competition:c}:{competition:Competition}){return <><div className="competition-header"><div><h2>{c.name}</h2><p>{c.season.name} · {c.division.name} · {c.format.name}</p></div><StatusBadge status={c.status}/></div><nav className="tabs">{[['Resumen','overview'],['Participantes','entries'],['Fixture','fixture'],['Progresión','progression']].map(([n,p])=><NavLink key={p} to={`/admin/competitions/${c.competitionId}/${p}`}>{n}</NavLink>)}</nav></>}
