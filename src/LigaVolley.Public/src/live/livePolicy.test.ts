import{describe,expect,it}from'vitest';import{freshness,normalDelay,retryDelay}from'./livePolicy';import type{Live}from'../api/types';
const live=(age:number,status:Live['status']='InProgress'):Live=>({matchId:1,status,home:{teamEntryId:1,teamName:'Miller',setsWon:2},away:{teamEntryId:2,teamName:'Olimpia',setsWon:1},sets:[],serverTime:'2026-08-26T12:05:00Z',lastUpdatedAt:new Date(Date.parse('2026-08-26T12:05:00Z')-age*1000).toISOString()});
describe('public livescore policy',()=>{it('classifies freshness using server time',()=>{expect(freshness(live(30)).label).toBe('EN VIVO');expect(freshness(live(31)).label).toContain('31s');expect(freshness(live(121)).tone).toBe('delayed')});it('never calls suspended live',()=>expect(freshness(live(10,'Suspended')).label).toContain('SUSPENDIDO'));it('uses required polling cadence',()=>{expect(normalDelay('InProgress')).toBe(5000);expect(normalDelay('Suspended')).toBe(15000);expect(normalDelay('Finished')).toBeNull()});it('backs off up to thirty seconds',()=>{expect([1,2,3,4].map(retryDelay)).toEqual([5000,10000,20000,30000])});it('marks finished without delayed freshness',()=>expect(freshness(live(999,'Finished'))).toEqual({label:'FINALIZADO',tone:'final'}))});
